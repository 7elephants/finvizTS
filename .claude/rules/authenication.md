API authentication is handled via a query parameter on all requests to the Finviz Elite API. The query parameter is called `auth` and its value is the API key provided by Finviz when you sign up for an Elite account.

**Example:** `https://finviz.com/export.ashx?auth=YOUR_API_KEY`
